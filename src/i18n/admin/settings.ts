import type { AdminModuleDict } from "./types";

// /admin/settings is a redirect-only alias route with no rendered UI strings.
export const settings: AdminModuleDict = {
  en: {},
  ru: {},
  es: {},
};
